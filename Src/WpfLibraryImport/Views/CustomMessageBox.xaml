<Window x:Class="WpfLibraryImport.Views.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLibraryImport.Views"
        mc:Ignorable="d"
        Title="WPF Sample" Height="400" Width="1200"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Border BorderBrush="Black" BorderThickness="1">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Height="45" Background="DarkBlue" MouseDown="TitleBar_MouseDown">
                <TextBlock Text="WPF Sample" FontSize="30" Foreground="White" VerticalAlignment="Center" Margin="10 0 0 0"/>
            </Border>

            <Grid Grid.Row="1" Margin="30 30 30 5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ContentControl x:Name="iconContainer" Grid.Column="0"/>
                <TextBlock x:Name="txtMessage" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="42"/>
            </Grid>

            <Button Grid.Row="2" Width="300" Height="90" Style="{StaticResource MahApps.Styles.Button.Square}" Click="OkButton_Click">
                <TextBlock Margin="15 0 15 0" FontSize="35" Text="확인"/>
            </Button>
        </Grid>
    </Border>
</Window>
